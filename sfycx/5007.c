#include<stdio.h>
int main(){
    int n;
    for(scanf("%d", &n);n!=0;scanf("%d", &n)){
        int basis[14][2]={0};

        if(){
            printf("Number %d is palindrom in basis", n);
            for()
        }
        else{printf("Number %d is not palindrom", n);}
    }
}