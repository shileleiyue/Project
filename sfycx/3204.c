
#include <stdio.h>

struct {
    int start;
    int end;
}zone;

void merge_intervals(struct zone zone[], struct zone zone[]
{
    /* data */
};
) {// 这里可以实现合并区间的逻辑
    
}

int main() {
    struct zone zone[100];
    int l,m;
    scanf("%d %d",&l,&m);
    for(int i=0;i<m;i++){
        scanf("%d %d",&zone[i].start,&zone[i].end);
    }

    return 0;
}